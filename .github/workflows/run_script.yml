name: Update Dashboard Data

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run-python-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests pandas openpyxl

    - name: Run Python script
      env:
        PYRAT_CLIENT_TOKEN: ${{ secrets.PYRAT_CLIENT_TOKEN }}
        PYRAT_USER_TOKEN: ${{ secrets.PYRAT_USER_TOKEN }}
      run: python build_dashboard.py --animals-live --licenses-live --valid-dates "license_list.csv" -o index.html

    - name: Commit and push updated HTML
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add index.html || exit 0
        git commit -m "Auto-update dashboard HTML" || exit 0
        git push
